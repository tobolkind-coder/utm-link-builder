<condense>
<context>
1. Previous Conversation:
  Пользователь столкнулся с проблемами при развертывании сервиса на VPS: "Permission denied" при вызове скриптов, сервис был недоступен, SSL-сертификат не выпускался корректно, а вывод скрипта установки не показывал адрес входа в админку.

2. Current Work:
  Были исправлены права доступа к файлам скриптов в git, добавлена генерация временного self-signed сертификата для корректного старта Nginx, исправлен вызов Certbot (добавлен --entrypoint) и обновлен скрипт `install.sh` для ожидания инициализации БД и вывода реального пути входа. Документация `INSTALL.md` обновлена с учетом этих изменений.

3. Key Technical Concepts:
  - Docker Compose: использование entrypoint для переопределения поведения контейнеров.
  - SSL/TLS: использование Let's Encrypt через Certbot (webroot challenge).
  - Безопасность Next.js: скрытие административного пути (/admin) и использование случайного пути входа (customLoginPath).
  - Git: управление правами файлов (chmod +x) в индексе репозитория.

4. Relevant Files and Code:
  - `install.sh`: Основной скрипт установки. Обновлен для ожидания БД и вывода пути входа.
  - `scripts/init-ssl.sh`: Исправлен вызов `docker compose run` с `--entrypoint certbot`.
  - `scripts/init-self-signed.sh`: Новый скрипт для создания временного SSL-сертификата.
  - `INSTALL.md`: Полностью переработана инструкция по установке.

5. Problem Solving:
  - Решена проблема прав доступа (git update-index --chmod=+x).
  - Решена проблема "курицы и яйца" при запуске Nginx с SSL (добавлен self-signed сертификат).
  - Решена проблема вывода пути админки (ожидание БД через psql).

6. Pending Tasks and Next Steps:
  - Все задачи выполнены. Пользователю даны команды для исправления текущего инстанса на VPS.
</context>
<task_progress>
- [x] Исправить scripts/init-ssl.sh (entrypoint override для certbot certonly)
- [x] Обновить install.sh: дождаться сидирования БД и вывести реальный URL страницы входа
- [x] Обновить INSTALL.md: объяснить механизм скрытого /customLoginPath, где его искать
- [x] Закоммитить и запушить изменения на GitHub
- [x] Дать пользователю команды для ремонта текущего VPS-инстанса
</task_progress>
</condense>